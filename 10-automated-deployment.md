# Automate cloud deployments

As a developer, I need automated deployment of new changes to the cloud so that approved changes can be delivered consistently and efficiently.

## Acceptance Criteria

- Given a new change is committed to the repository
- When the deployment workflow is triggered successfully
- Then the change is automatically deployed to the cloud environment.
